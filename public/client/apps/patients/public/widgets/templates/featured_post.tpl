<div>
    <img src="/assets/images/ldw_6543.jpg" height="410" width="340"/>
    <h4 class="ui header">
        <%=obj.title%>
    </h4>
    <p>
        In a promise chain, when error occured in middle promise action, following actions not executed, the error is
        passed through to the latter promises that has an error handler. If all latter promises have not error handler,
        error is ignored.
    </p>
</div>

